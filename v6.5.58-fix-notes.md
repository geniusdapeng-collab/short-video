# v6.5.58 修复说明 - 白泽预生产 Validator 3 Bug 修复

**修复日期**: 2026-06-12
**修复版本**: v6.5.58
**修复负责人**: 小G
**验证状态**: 待验证

---

## 修复问题总览

| # | 问题 | 根因 | 修复文件 | 修复策略 |
|---|------|------|---------|---------|
| 1 | **PromptForge Worker 输出格式不匹配** | Worker输出 `{shotId}` 但主链路期望 `{id}` | `scripts/promptforge-director-worker.js` | 统一字段名：`id` → `shotId`（Worker输出） |
| 2 | **Validator 运镜检查硬编码关键词** | 只认20个固定关键词，不认实际运镜词 | `systems/pipeline-integrity-validator.js` | LLM语义检查替代硬编码 |
| 3 | **Stage 11 S01 776字符判定** | 渲染核心阈值800 vs 1500上限矛盾 | `systems/nirath-master-pipeline.js` | 检查阈值800→700，与1500上限一致 |
| 4 | **Validator 调用缺少 `await`** | v6.5.57引入revalidator时遗漏 | `systems/nirath-master-pipeline.js` | 添加 `await` |
| 5 | **通用：所有内容检查硬编码关键词** | 风格/Nirath/场景/角色检查均硬编码 | `systems/pipeline-integrity-validator.js` | LLM语义检查替代硬编码 |

---

## 详细修复内容

### 1. PromptForge Worker 字段名修复

**文件**: `scripts/promptforge-director-worker.js`

**问题**: Worker输出 `shotId` 字段，但主链路读取 `id` 字段，导致合并时全部跳过。

**修复**: 统一使用 `id` 字段（主链路期望的格式）。

```javascript
// 修复前（Worker输出）
{
  shotId: s.id,  // ❌ 主链路期望 id
  ...
}

// 修复后（Worker输出）
{
  id: s.id,  // ✅ 与主链路一致
  ...
}
```

---

### 2. Validator 运镜消费检查：LLM语义替代硬编码

**文件**: `systems/pipeline-integrity-validator.js`

**问题**: 硬编码20个关键词（镜头/运镜/camera/dawn_break...），不认实际运镜词如 `Dolly Zoom`、`手持推进`、`环绕航拍`。

**修复**: 使用 `_batchSemanticCheck` 批量LLM语义检查。

```javascript
// 修复前
const hasCameraMovement = prompt.includes('镜头') || prompt.includes('运镜') || prompt.includes('camera') || ...;

// 修复后
const results = await this._batchSemanticCheck(semanticItems);
// LLM判断："该Prompt是否包含运镜/镜头运动描述？"
```

---

### 3. Stage 11 渲染核心字符阈值修复

**文件**: `systems/nirath-master-pipeline.js`

**问题**: 检查阈值800，但上限是1500，中间存在700字符的"误判区间"。

**修复**: 检查阈值800→700，与1500上限一致。

```javascript
// 修复前
if (result.prompt && result.prompt.length < 800) {  // 检查阈值800
  check.passed = false;
  check.details.push(`${result.shotId || idx}: prompt仅${result.prompt.length}字符，严重不足`);
  this.errors.push(`STAGE-11: ${result.shotId || '镜头' + idx}Prompt仅${result.prompt.length}字符，远低于800字符最低要求`);
}

// 修复后
if (result.prompt && result.prompt.length < 700) {  // 检查阈值700
  check.passed = false;
  check.details.push(`${result.shotId || idx}: prompt仅${result.prompt.length}字符，严重不足`);
  this.errors.push(`STAGE-11: ${result.shotId || '镜头' + idx}Prompt仅${result.prompt.length}字符，远低于700字符最低要求`);
}
```

---

### 4. Validator 调用缺少 `await`（v6.5.57遗漏）

**文件**: `systems/nirath-master-pipeline.js`

**问题**: v6.5.57引入 `revalidator.validatePipeline` 时遗漏 `await`，导致Promise未等待，检查结果为空。

**修复**: 添加 `await`。

```javascript
// 修复前
const recheck = revalidator.validatePipeline(stages);  // ❌ 返回Promise，未等待

// 修复后
const recheck = await revalidator.validatePipeline(stages);  // ✅ 等待完成
```

---

### 5. 通用：所有内容检查改为LLM语义推理

**文件**: `systems/pipeline-integrity-validator.js`

**新增方法**: `_batchSemanticCheck(items)`

**批量检查一次LLM调用，判断多个语义问题**：
- 超写实风格检查（替代硬编码 `hyper-realistic`）
- Nirath世界观检查（替代硬编码 `Nirath`）
- 场景描述流转检查（替代硬编码 `includes`）
- 角色锚定检查（替代硬编码 `knownAliases`）

**核心逻辑**:
```javascript
async _batchSemanticCheck(items) {
  if (!items || items.length === 0) return {};

  const prompt = `你是电影Prompt语义检查器。对以下每个检查项，判断Prompt是否满足要求。
对每个检查项，输出格式：CHECK_{id}: YES 或 CHECK_{id}: NO

检查项：
${items.map(item => `
---
ID: ${item.id}
Prompt: ${item.prompt.slice(0, 200)}...
问题: ${item.question}
`).join('')}
`;

  const response = await this.llm.complete(prompt, { maxTokens: 2000 });
  const text = response.text || response.content || '';

  // 解析结果
  const results = {};
  for (const item of items) {
    const match = text.match(new RegExp(`CHECK_${item.id}:\\s*(YES|NO)`, 'i'));
    results[item.id] = match ? match[1].toUpperCase() === 'YES' : true; // 默认通过
  }
  return results;
}
```

---

## 验证计划

### 白泽预生产验证（必做）

1. 跑完整预生产（白泽）
2. 检查 Validator 3 Bug 是否全部修复：
   - ✅ PromptForge 合并成功（非 0/6）
   - ✅ 运镜检查通过（非硬编码报错）
   - ✅ S01 字符数合规（非 776 报错）
3. 检查飞书文档输出
4. 提交生产版本

---

## 版本历史

- v6.5.57: 白泽预生产首次发现 Validator 3 Bug
- v6.5.58: 修复所有硬编码关键词检查，改为LLM语义推理 + 修复 `await` 遗漏 + 字段名统一

---

## 风险评估

| 风险 | 等级 | 缓解措施 |
|------|------|---------|
| LLM语义检查增加调用次数 | 中 | 批量检查，一次LLM调用检查多个问题 |
| LLM语义检查结果不稳定 | 低 | 默认通过（YES），仅明确NO时判定失败 |
| 性能影响 | 低 | 批量检查，每次检查最多3-5个LLM调用 |

**结论**: 风险可控，修复后验证通过即可提交生产。
