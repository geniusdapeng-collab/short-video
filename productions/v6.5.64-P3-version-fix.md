# 版本号修正记录

## 问题
- `.current-version` 文件显示 `v6.6.5`，但实际代码已回退到 `v6.5.64-P3`
- 自 `v6.5.64-P2`（06ed229）起，所有后续提交均未更新 `.current-version`

## 根因
- 6月15日外部专家修复回退到 v6.5.64-P2 时，提交遗漏了 `.current-version` 文件
- 从 v6.5.64-P2 到 P3 的 5 个后续提交，均未同步版本号

## 修复
- 将 `.current-version` 从 `v6.6.5` 修正为 `v6.5.64-P3`
- 补交 git commit，使版本号与代码一致

## 时间
2026-06-16 08:18
