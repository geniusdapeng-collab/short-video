# v6.5.0 生产发布记录

**发布时间**: 2026-06-07 01:10 CST
**版本号**: v6.5.0
**Git Commit**: efe5957
**提交者**: AgentX (自动发布)

## 本次发布内容

### 架构升级 (B-1 ~ B-7)
- **B-1**: 统一基础设施收口 (CLI + Logger + Error + Env + OutputCleaner + ReportWriter + PreproductionService)
- **B-2**: 统一渲染提交核心 (PromptResolver + PortraitResolver + RenderRequestBuilder + RenderSubmitter)
- **B-3**: 统一质量评估核心 (QualityDimensions + QualityReporter + QualityGate)
- **B-4**: 统一 Stage 编排壳 (StageContext + StageResult + StageRunner)
- **B-5**: 核心 5 Stage 服务化 (Script/Duration/Storyboard/Camera/RenderPrep)
- **B-6**: 统一配置中心 (CapabilityRegistry + ModeProfile + StageMap)
- **B-7**: 文档真源收口 (SystemManifest + ReleaseManifest + HealthCheck + SystemSummary)

### 增量改造
1. **run-taotie-preproduction.js** → 薄 wrapper (66行)，统一调用 `systems/preproduction-service.js`
2. **nirath-master-pipeline.js** → 接入 StageRunner + StageService + QualityGate
   - 5 个核心阶段接入 StageRunner 追踪 (STAGE-5/6/7/9/11-RUNNER)
   - 成功路径 + 异常路径双保险 QualityGate 评估
3. **QualityGate** → 6 维质量模型 (完整性/正确性/一致性/丰富度/安全性/效率)

### 修复记录
- **Fix 1**: `stories/taotie-ep01-input.json` 中文引号 → 日式引号，避免 JSON 解析错误
- **Fix 2**: 添加 `targetDuration` 字段兼容 Stage 2 需求对齐检查
- **Fix 3**: `nirath-master-pipeline.js` 恢复 `NirathVisualAnchorInjector` 导入（被误删）
- **Fix 4**: 清理测试文件 (preproduction-run-*.log, backup-*.md, audit-*.json)

## 验证结果
- ✅ 语法检查: 全部通过 (node -c)
- ✅ 模块加载: preproduction-service.js, nirath-master-pipeline.js 正常 require
- ✅ 健康检查: 42/42 通过
- ✅ 全量打包: 877 文件, 13.9MB

## 发布检查清单
- [x] 所有修改已 git commit
- [x] `.current-version` 已更新为 6.5.0
- [x] `package.json` 版本已更新为 6.5.0
- [x] `config/system-manifest.json` 版本已更新
- [x] 发布记录已归档

## 状态
🟢 **已发布** | 准备跑预生产验证
