
### 严重事故：需求清单输出不规范（2026-06-14 14:40）
**事故等级**：系统级错误
**后果**：队长严重不满，信任受损

**事故描述**：
- 队长已明确规范：每次需求确认必须输出完整的七大章节28个字段
- 规范已写入SOUL.md、AGENTS.md、memory
- 但我仍然犯了：只输出部分字段，让队长"选择A/B/C"
- 严重偏离规范，属于"明知故犯"

**深度根因分析**：
1. **流程理解错误**：把"清单输出"变成了"状态汇报+选择题"
2. **角色定位错误**：把自己当成"智能助理"而非"机械执行器"
3. **习惯性回复模式**：旧习惯（给选项）覆盖新规范（出清单）
4. **记忆与执行断层**：知道规范但没有建立"硬约束检查点"

**根治方案（三层防护）**：

### 第一层：触发器硬约束
- 规则：任何消息触发需求确认 → 强制输出完整28个字段
- 自检口诀："看到需求确认 → 立即输出七大章节 → 一字不少 → 等待确认"
- 违反 = 失败回复

### 第二层：格式检查清单
每次回复前必须检查：
1. ✅ 是否包含七大章节？
2. ✅ 是否28个字段都有？
3. ✅ 是否有字段被省略或简化？
4. ✅ 是否只是"汇报"而不是"完整清单"？
- 任何一项否 = 重新生成完整清单

### 第三层：记忆条件反射
- 将本次事故写入记忆
- 形成条件反射：提到"需求确认" → 立即联想到"七大章节28字段"
- 每次执行前默念："不输出完整清单 = 系统级错误"

**执行要求**：
- 此机制永久有效，不可绕过
- 任何情况下，需求确认 = 完整清单
- 违反 = 系统级错误，立即上报队长

**记忆时间**：2026-06-14 14:45


---

## 2026-06-14 21:00 - 预生产流水线失败深度排查（进行中）

### 排查背景
队长要求系统性排查最近7次预生产运行的失败问题，要求"一次只解决一个问题，静下心来深度分析"。

### 关键发现1：PromptForge 子进程不在 Stage 审计范围内 🔥
**发现时间**：2026-06-14 17:00
**文件**：`nirath-master-pipeline.js` line ~6650+

代码结构：
```javascript
result.stages.compliance = await runStage('STAGE-12', () => this.stageCompliance(...));
// 【真空地带】PromptForge 子进程在这里跑，但不在 runStage 内！
const worker = spawn('node', ['--max-old-space-size=8192', ...]); // 1800秒超时
result.stages.preRender = await runStage('STAGE-13', () => this.stagePreRenderValidation(...));
```

**影响**：
- Stage-13/14/15 的 0ms 审计耗时是正常的（CPU数据整理）
- 真正的重活在 Stage-12 和 Stage-13 之间的"真空地带"
- `mild-sho` 的 66 分钟间隙（4018885ms）= PromptForge 子进程执行时间
- 历史基线 ~25s 已失效（当时 PromptForge 在主进程内，现在抽离到子进程）

### 关键发现2：~120秒模式（3个进程在约120s后终止）
**发现时间**：2026-06-14 17:30
**审计文件**：
- `execution-audit-exec-1781420558258-ad721e5e.json`：STAGE-11.5 耗时 120,090ms 后终止
- `execution-audit-exec-1781421935821-cfd80553.json`：STAGE-12 耗时 120,106ms 后终止
- `execution-audit-exec-1781422694498-024c348d.json`：STAGE-14 耗时 120,095ms 后终止

**模式**：所有终止点都在约120秒（120,000ms±），未进入后续stage。

**根因方向**：
- `stageCompliance` 和 `OrientPrimordialCoreV24.enforce()` 本身为轻量字符串操作（无LLM）
- `OrientPrimordialCoreV24` 构造函数内惰性加载7个重模块：`WorldArchaeologistAgent`、`NarrativeAnalystAgent`、`CinematicInstructionBuilder`、`NirathStyleEnforcer`、`BackgroundEnrichmentAgent`、`DualStarLightingEngine`、`NirathMaterialSystem`
- 第一次调用 `enforce()` 时可能触发 `this.modules` 的惰性初始化，导致2-3分钟延迟

### 关键发现3：OOM 根因定位
**根因**：子进程内存限制 `--max-old-space-size=8192`（8GB），但系统总内存仅 7.5GB。
- `promptforge-director-worker.js` 内 `maxTokens: 16000`
- 16K tokens 的 reasoning content 占用大量内存
- V8 堆 + 系统 RSS 重叠，Linux OOM killer 直接 `SIGKILL`

**影响进程**：`calm-slu`（SIGKILL）、`fresh-ti`（SIGTERM）

**队长指示**：OOM已有历史解决方案（子进程隔离+单镜头串行+maxTokens=700），无需重新解决。

### 关键发现4：`input is not defined`（glow-riv，Stage-15）
**排查结果**：
- `stagePostProduction` 方法体内无任何裸 `input` 变量引用
- `runStage` wrapper 无 `input` 引用
- `_injectCreativeIntensity` 无 `input` 引用
- Node.js 脚本全量扫描确认：所有裸 `input` 均出现在以 `input` 为形参的方法/闭包内，无悬空引用
- 构造函数无 getter/proxy 引用 `input`

**结论**：静态分析无法定位。需写 reproduction test 捕获运行时完整堆栈。

### 关键发现5：其他异常点
| 进程 | 异常 | 历史基线 | 当前值 |
|------|------|---------|--------|
| `vivid-fo` | Stage-9 | 1056ms | 1ms |
| `cool-lag` | Stage-11.5 | 34249ms | 1ms |
| 多个进程 | Stage-7 | ~200ms | ~1ms |

### 关键发现6：构造函数隐患
`NirathMasterPipeline` 构造函数中未执行 `this.options = options`，但 `stagePostProduction` 使用 `this.options?.isPreProduction` 进行可选链访问。虽然不会报错（undefined?.prop = undefined），但逻辑上可能跳过某些分支。

### 关键发现7：`cool-lag` 和 `mild-sho` 共享同一 outputHash
`cool-lag` STAGE-12 outputHash = `04dc16fddf21a5ef`
`mild-sho` STAGE-14 outputHash = `04dc16fddf21a5ef`

可能原因：对象引用复用或相同的空/默认结果。

### 待深入靶子（按优先级）
1. **Stage-12 耗时异常**（175s/120s）：追查 `OrientPrimordialCoreV24.enforce()` 的惰性初始化成本
2. **`input is not defined`**：写 reproduction test pinpoint exact 位置
3. **Stage-11（43ms）**：检查 `stageRender` 是否短路
4. **Stage-7（~1ms）**：检查 storyboard 生成是否真实执行

### 队长最新指示
2026-06-14 21:04："OOM这个我们之前解决过...其他的你按照你说的建议深入解决下"
→ 确认不重新解决OOM，全力深入其他问题。

