# 2026-06-15 工作日志

## 外部专家介入：按问题分批打包代码

### 背景
- 大鹏发送专家反馈文件（CFNetworkDownload），指出之前9.3MB大包被截断，建议：
  - 按问题分4批打包
  - 每批仅3-8个相关文件
  - 带上调用链和日志
  - 每个文件必须完整，不要只截函数中间

### 关键发现：实际生效代码路径
- 入口脚本 `run-health-edu-ep01.js` 引用：`./zhuoyue-system/core/nirath-master-pipeline.js`
- **实际生效版本**：`/root/.openclaw/workspace/zhuoyue-system/core/nirath-master-pipeline.js`（9243行，v1.1）
- **非生效版本**：`/root/.openclaw/workspace/systems/nirath-master-pipeline.js`（9488行）
- 后续所有打包必须基于实际生效版本

### 已发送批次

#### 第1批：问题1/2/3
- 文件：`/root/.openclaw/workspace/output/第1批-问题1-2-3-完整代码.zip`
- 包含：
  - 问题1：Stage -1 需求确认流程未正确中断（返回 REQUIREMENT_CONFIRMATION_REQUIRED 但继续执行）
  - 问题2：LLM content 为空 + JSON 解析失败（reasoning_content 兜底，但内容非合法 JSON）
  - 问题3：主进程无子进程模式，但系统使用子进程（`--max-old-space-size=8192`，1800s 超时）
- 关键代码：run-health-edu-ep01.js、zhuoyue-system/core/nirath-master-pipeline.js execute 方法、systems/llm-reasoning-engine.js

#### 第2批：问题4/7
- 文件：`/root/.openclaw/workspace/output/第2批-问题4-7-角色系统.zip`
- 包含：
  - 问题4：角色服装推断错误（入口脚本定义 `outfit='警服'`，但 `_getDefaultClothing()` 根据 `role='nurse'` 返回 `白色护士服`）
  - 问题7：角色档案缺乏 outfit 字段，且 `v2Metadata.minimalAnchor` 包含 Nirath 残留
- 关键代码：systems/character-prompt-builder.js、systems/character-manager-v2.js、characters/chen-nurse/character-card.json

#### 第3批：问题5/6
- 文件：`/root/.openclaw/workspace/output/第3批-问题5-6-Nirath残留与Prompt长度.zip`
- 包含：
  - 问题5：Nirath 描述渗漏（generic 模式下角色 minimalAnchor 仍含 `Nirath异兽, Nirath原生特征, 双恒星光照反射`）
  - 问题6：Prompt 长度限制矛盾（config/prompt-length.js 定义 HARD_MAX=988，但实际代码使用 1500）
- 关键代码：config/prompt-length.js、systems/prompt-pipeline-bridge.js、zhuoyue-system/core/nirath-master-pipeline.js smartTrim/finalFillPrompt

### 待办
- [ ] 第4批（问题8：其他问题）——等待大鹏指示是否继续
- [ ] 等待外部专家反馈
- [ ] 根据专家反馈修复系统

### 关键日志证据
- debug_llm/ 目录下大量 `empty_content_reasoning` 和 `json_extract_fail_content` 日志
- 预生产结果中每个镜头 Prompt 长度正好 1500 字符（被截断）
- 系统日志显示 "角色系统(v2 + Nirath增强)" 即使在 generic 模式下

## 其他
- 当前时间：2026-06-15 12:43 CST
- 模型：kimi/k2p6
- 通道：feishu


---

## 外部专家反馈落地：7类问题全部修复完成

### 修复时间
2026-06-15 15:00-16:35 CST

### 修复范围（按问题编号）

| 问题 | 描述 | 状态 | 修改文件 |
|------|------|------|----------|
| 1 | Stage -1 需求确认流程 | ✅ | `run-health-edu-ep01.js` — 加 `REQUIREMENT_CONFIRMATION_REQUIRED` 判断 + 提前 return |
| 2 | LLM content 为空 + JSON 解析失败 | ✅ | `systems/llm-reasoning-engine.js` — `forceJson` 模式、`response_format: {type:'json_object'}`、temp=0.1、禁止 reasoning_content 兜底 |
| 3 | PromptForge 子进程内存过大 | ✅ | `zhuoyue-system/core/nirath-master-pipeline.js` — `PROMPTFORGE_MAX_OLD_SPACE_MB` 环境变量、默认 1536MB、不再 8192MB |
| 4 | 角色服装推断优先级 | ✅ | `systems/character-prompt-builder.js` — 显式 outfit 优先级链：`appearance.clothing.promptFragment` > `visualIdentity.outfit` > `visual.outfit` > `character.outfit` > role fallback |
| 5 | Nirath 描述渗漏 | ✅ | `systems/character-manager-v2.js`、`zhuoyue-system/core/nirath-master-pipeline.js` — `_buildMinimalAnchor()` 默认物种 `human/人类`、generic 模式 sanitize、日志去 "Nirath" |
| 6 | Prompt 长度硬编码 1500 | ✅ | `zhuoyue-system/core/nirath-master-pipeline.js`、`systems/prompt-pipeline-bridge.js` — 全量替换为 `PROMPT_LENGTH.HARD_MAX(988)`，共 69 处替换 + 7 处模板字符串修复 |
| 7 | 角色档案缺少 outfit + Nirath 残留 | ✅ | `characters/chen-nurse/character-card.json`、`systems/character-manager-v2.js` — 重写角色卡（`role: presenter`、`species: human`、outfit 写入）、`mergeRuntimeCharacterData()` 运行时覆盖 |

### Pipeline 核心方法修复

#### `stageCharacters()` (Stage-4)
- 调用 `mergeRuntimeCharacterData()` 进行运行时数据合并
- generic 模式调用 `_sanitizeCharacterForGenericMode()` 清洗 Nirath 残留
- outfit 兜底修正

#### `stageRender()` (Stage-11)
- 新增 `_buildRenderMetaForShot()` — 构建角色锚点元数据
- 新增 `_mergeCharacterAnchorIntoPrompt()` — 将角色锚点注入最终 prompt
- 强制服装描述必须出现

#### `stagePromptQualityGate()` (Stage-11.5)
- 新增 `_checkShotOutfitPresence()` — 检查服装命中
- 新增 `_collectForbiddenKeywordsForMode()` — generic 模式收集禁用词
- 新增 `_repairShotPromptByQualityGate()` — 自动修复不合规 prompt
- 长度校验使用 `PROMPT_LENGTH.HARD_MAX`

#### 其他 helper
- `_sanitizePromptForGenericMode()` — generic 模式全链路 sanitize
- `_releaseMemory(result)` — Stage-11 后释放主进程内存防 OOM
- `finalFillPrompt()` — 改为使用 `PROMPT_LENGTH.TARGET_MAX` 和 `PROMPT_LENGTH.HARD_MAX`
- Stage 11.5 调用传入 `result.stages` 并回写 `result.stages.render`

### PromptForge 子进程修复
- 内存：`--max-old-space-size=8192` → `--max-old-space-size=${workerMemoryMb}`（默认 1536MB）
- 退出码：捕获 `{code, signal}`，增加 `code === 137` 和 `signal === 'SIGKILL'` 的 OOM 检测
- `finally` 块：`unlinkSync(inputFile/outputFile)` 清理临时文件
- 合并回灌：`_cleanForgePrompt` 后再次注入 `renderMeta.mergedCharacterAnchor` + `_sanitizePromptForGenericMode()` + `smartTrim`

### 自检脚本
- 新建 `tools/check-project-fixes.js`
- 覆盖 7 大类别检查 + Nirath 残留词扫描
- 运行结果：**全部通过**（0 FAIL, 0 WARN, 全绿）

### 下一步
- 运行一次完整预生产链路验证（`health-edu-ep01-rhabdomyolysis`）
- 重点验证 Stage-4（角色）、Stage-11（渲染）、Stage-11.5（质量门）输出
- 如有异常，继续按专家模式修复
