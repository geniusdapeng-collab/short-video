
---

## 生产版本发布：v6.5.55 - 修复prompt-standardizer.js缺失函数

**时间**：2026-06-11 23:55（GMT+8）
**提交**：01ccb3a
**推送**：master → zhuoyue (GitHub)
**改动**：2 files changed, 43 insertions(+), 1 deletion(-)

**修复内容**：
- `prompt-standardizer.js` 补全3个缺失函数：
  1. `applyStandardizedPromptToShot` — 桥接层将标准化结果写回shot对象
  2. `standardizePromptObject` — 统一标准化入口（兼容自然语言/块格式/key:value）
  3. `getPromptFromShot` — 从shot对象按优先级读取prompt（render_prompt → renderPrompt → prompt → visualPrompt → standardizedPrompt）

**触发场景**：v6.5.54专家方案落地后，白泽预生产验证失败报错 `applyStandardizedPromptToShot is not a function`，根因为P0新建文件时遗漏了3个接口函数。

**验证**：修复后白泽预生产成功通过，QualityGate 68分/WARN，Stage 12合规率83%，无OOM/SIGKILL。

**版本链**：v6.5.53-l → v6.5.54（专家方案落地）→ v6.5.55（修复缺失函数）
